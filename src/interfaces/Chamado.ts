// TODO (Critério 14): Defina as interfaces TypeScript para as entidades do sistema.

// Interface para o Chamado
// Atributos: id (number), titulo (string), descricao (string),
//            status (string), prioridade (string),
//            dataCriacao (string), tecnicoId (number)
export interface Chamado {
  // TODO: adicione os atributos aqui
}